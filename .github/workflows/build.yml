name: Build master

on:
  push:
    branches:
      - 'master'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - uses: actions/setup-java@v2
        with:
          java-version: 11
          distribution: adopt
      - name: Build & Deploy
        shell: bash
        run: |
          mvn deploy --settings ./settings.xml --batch-mode -DdeployAtEnd=true
          echo "Deployed artifacts"
        env:
          GITHUB_PASSWORD: ${{ secrets.GITHUB_TOKEN }}